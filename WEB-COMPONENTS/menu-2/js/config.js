const config = {
    position: 'right',  // Options: 'left', 'right'
    defaultMode: 'large',  // Options: 'large', 'intermediate', 'reduced'
    animationDuration: 300,  // Durée de l'animation en millisecondes
};

export default config;